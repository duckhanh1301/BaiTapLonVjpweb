const SectionPage = ({ name }) => (
    <div className="section-page">
        <p className="text-muted mb-0">
            Nội dung trang {name} sẽ được hiển thị tại đây.
        </p>
    </div>
);

export default SectionPage;
